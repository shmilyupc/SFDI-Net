# Data Directories

Create the following subdirectories as needed:

- `train_images_with_gaps/`
- `train_masks/`
- `test_images/`
- `test_masks/`

Training expects paired image-mask data. Mask files should follow the
`xxx_mask.png` naming convention.
